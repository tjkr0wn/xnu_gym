#include <pongo.h>
#include <defs.h>

//TODO
void tfp0_all_callback(uint32_t *err) {
  *err += SUCCESS;
  return;
}

/*
  Description:
    Finds task_conversion_eval sequence and returns the start.
*/
//TODO: Convert return type to a ptr
static bool find_task_conversion_eval() {
  return true;
}

static bool find_inline_task_for_pid_check() {
  return true;
}

static bool find_task_for_pid_posix_check() {
  return true;
}
