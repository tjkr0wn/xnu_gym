#include <pongo.h>
#include <defs.h>

//TODO
void tfp0_all_callback(uint32_t *err) {
  *err += SUCCESS;

  return;
}

/*
  Description:
    Finds task_conversion_eval sequence and returns the start.
*/
static bool find_task_conversion_eval() {
  return true;
}
